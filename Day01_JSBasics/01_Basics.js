console.log("Welcome to learn Playwright with JavaScript!");
// above code is used to print a message in the console, 
// which is useful for debugging and logging purposes.


let x = 10; //Storing value in x variable, this variable value is can be 
// changed later in the code, so we can say that this variable is mutable.

const y = 20; //Storing value in y variable, 
// this variable value cannot be changed later in the code,
// so we can say that this variable is immutable.