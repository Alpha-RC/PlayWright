let fruits = []; //empty array

let fruits_fresh = ["apple","banana","Cherry"]; // Array with values
// length of above array is 3

let arr = [10,20,30,40]; // index:0-3 & length = 4

// to access the elements of array using index
console.log(arr[1]);
console.log(arr[3]);
console.log(arr[4]); // output: undefined as there is no index 4 present in array

let testResult = ["pass","fail","pass","skip"];

let mixedArr = [1, "hello", true, null]; // Arrays can hold Mixed datatype values

