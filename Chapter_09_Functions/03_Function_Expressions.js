// function as expression:

// Declare a function and store it in a varibale

const greet = function (name) {
    return `Hello ${name}`
};

// callin the function:
let r = greet("Rohit!"); // as this function returns something, need to store it in varibale
console.log(r); // output: Hello Rohit!
