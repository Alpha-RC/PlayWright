export let baseUrl  = "https://vwo.com";

export function formatUpperCaseString(name){
    return name.toUpperCase();
}

// for above 2 statements we have make them as export it means we can export them anywhere in the 'LEARNPLAYWRIGHTWITHAI' folder